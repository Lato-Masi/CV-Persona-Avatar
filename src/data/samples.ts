import { ProfileAnalysisResult, SampleProfile } from '../types';

export const SAMPLE_PROFILES: SampleProfile[] = [];

export const MOCK_PREBUILT_ANALYSES: Record<string, ProfileAnalysisResult> = {};
